streamlit==1.40.0
pandas==2.2.3
google-cloud-bigquery==3.27.0
google-cloud-storage>=2.18,<3
db-dtypes==1.3.0
psycopg2-binary==2.9.10

# OpenTelemetry → telemetry.googleapis.com (the new unified OTLP endpoint).
# Codelab Q2E-2 wires these via setup_otel() in app.py + OTEL_ENABLED env var.
# Note: google-cloud-bigquery's tracing is built in (no separate
# opentelemetry-instrumentation-google-cloud-bigquery package on PyPI), so only
# the OTel core + OTLP exporter are needed.
opentelemetry-api>=1.39
opentelemetry-sdk>=1.39
opentelemetry-exporter-otlp-proto-grpc>=1.39
google-auth>=2.35
